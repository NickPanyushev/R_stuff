#ifndef _SYMBOLS_H
#define _SYMBOLS_H

#include "stdinc.h"

extern char numToChar(int num);
extern int charToNum(char alpha);

extern bool isNucleotide(int num);
extern bool isChar(char c);

extern int complChar(int num);

#endif
